FROM node:18-alpine

WORKDIR /app

# Install deps first (cache-friendly)
COPY frontend/package.json ./
RUN npm install

# Copy source
COPY frontend .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]
